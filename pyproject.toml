[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ibvap"
version = "0.1.0"
description = "Intelligent Border Video Analytics Platform – SIH 2026"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "ultralytics>=8.3",
    "opencv-python>=4.10",
    "numpy>=1.26",
    "pyyaml>=6.0",
    "nvidia-ml-py>=12.0",
    "psutil>=6.0",
    "supervision>=0.24",
    "easyocr>=1.7.0",
    "websocket-client>=1.8",
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov",
    "ruff>=0.5",
]
# Phase 7: ONNX Runtime backend (GPU-accelerated inference)
onnx = [
    "onnxruntime-gpu>=1.18",
]
backend = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "websockets>=12.0",
    "pydantic>=2.0",
    "supabase>=2.0",
    "sqlalchemy>=2.0",
    "pg8000>=1.31",
    "alembic>=1.13",
]

[tool.hatch.build.targets.wheel]
packages = ["apps", "cv", "intelligence", "pipelines"]

[project.scripts]
ibvap-edge = "apps.edge.main:main"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v"
